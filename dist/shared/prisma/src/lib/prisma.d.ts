export { PrismaClient, Prisma, TenantStatus, UserRole, InvitationStatus, Department, NoteSource, NoteType, NoteStatus, } from '@prisma/client';
export type { Platform, TenantRegistry, Tenant, User, Invitation, Note } from '@prisma/client';
