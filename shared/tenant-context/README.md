# tenant-context

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tenant-context` to build the library.

## Running unit tests

Run `nx test tenant-context` to execute the unit tests via [Jest](https://jestjs.io).
